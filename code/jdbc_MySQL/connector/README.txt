How to run:

suppose we run it from `src/`
we could use the following:

```shell
javac -classpath ../lib/mysql-connector-java-8.0.15.jar:. Check.java 
java -classpath ../lib/mysql-connector-java-8.0.15.jar:. Check
```